package php

import com.caucho.quercus.env._;

trait Constants extends script {
  //%constants%
  
  /*
  def define(n: String, v: String): Boolean = {
    constants(n)=v
    return true;
  }
  def define(n: String, v: Boolean): Boolean = {
    constants(n)=v
    return true;
  }
  def defined(n: String): Boolean = {
    if (constants.contains(n))  return true;
    else return false;
  }
  */
}
